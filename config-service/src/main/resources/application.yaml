server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/JehisonBustamante/MTISW_EV2_Backend.git
          search-paths: config-data
  application:
    name: config-service
